Vodminer — running log
Format: [YYYY-MM-DD HH:MM] <commit-id> — <summary>
Append latest entries at the bottom.

[2026-05-24 12:02] 8097569 — Initial commit: add project plan and CLAUDE.md.
[2026-05-24 12:04] e7ab56d — Update plan doc: revise overview and move credentials to git secrets.
[2026-05-24 12:30] — Merged universal rules into CLAUDE.md. Created MEMORY.md, ERRORS.md, runninglog.txt, .claude/commands/deferred.txt. Deleted Uni-CLAUDE.md.
[2026-05-24 14:00] — Phase 2 scaffold via 8 sub-agents (Systems Architect → Backend/Twitch/ffmpeg/Discord/TikTok/QA/DevOps in parallel). Detector deferred to Phase 1 data. Lead reconciled index.js: createEventSubRouter()→{router,emitter}, getLatestVod, processVideo(highlight,sourcePath), reviewBot singleton, post(job,clip) via clipById map. All 18 source/test files parse via node --check. npm install not run.
[2026-05-24 15:30] — Pivot: TikTok app review failed. Removed all TikTok automation. Deleted src/tiktok/ (auth/uploader/poster), src/scheduler/queue.js, tests/tiktok/, tests/scheduler/, tests/__fixtures__/tiktokResponses.js. Pipeline now ends at Discord: VOD→detector→ffmpeg crop→Discord file attachment. Implemented clipDetector.js (audio-RMS spike detection, yt-dlp piped to ffmpeg s16le mono, mean+2σ threshold, group within 8s, ±10s preroll, top 15 by score, 30s clips). Dropped Approve/Reject buttons from reviewBot — sendPreview now posts embed + file only (status: 'delivered'). Kept 9:16 1080x1920 output, kept privacy-policy.html + terms-of-service.html. All 12 src + 9 test files parse via node --check.
[2026-05-24 17:30] — Live integration L1-L4 passed. Wrote .env (gitignored) with creds. winget installed ffmpeg/ffprobe/yt-dlp/ngrok. Wrote 4 test scripts: test-twitch.js (creds+Helix ✅), test-discord.js (bot ping ✅ after OAuth invite to Vodminer server #clip-reviews), test-detector.js (audio-RMS on real VOD 2778567108 — 5 highlights, top score 3.81 ✅), test-pipeline.js (full fake-trigger). Bug found and fixed: ffmpegPipeline.js was double-trimming — yt-dlp pre-trims segments to [start,end] starting at t=0, then ffmpeg.setStartTime(highlight.startSec) was seeking past EOF. Removed setStartTime; kept setDuration(maxDurationSec) as safety cap. After fix: 5/5 highlights delivered to Discord in 70.8s. Extracted runPipeline from index.js into new src/pipeline.js for testability. Wrote scripts/register-eventsub.js for L5 webhook bootstrap.
[2026-05-24 18:30] — L5 live and enabled. ngrok 3.3.1 (winget) too old; ran ngrok update to 3.39.4. Fixed config schema mismatch (v3 vs v1/v2). Fixed healthcheck.js using -version (yt-dlp rejects, parses as -v -e -r -s -i -o -n); switched to --version + ENOENT detection. EventSub subscription 15a18eef-2aaf-498e-8812-884a7ea2094e enabled for stream.offline. Created /vodminer slash command (.claude/commands/vodminer.md) — idempotent boot of full stack.
[2026-05-24 19:00] — Phase A backfill feature. Config: clipLengthSec→minClipLengthSec(45)/maxClipLengthSec(60) + postRollSec(5); detector now picks per-clip length from spike duration + preroll/postroll, clamped to [45, 60]. Added vodFetcher.getAllVods() with Helix pagination (100/page). New src/pipeline.js exposes processVod(vod, opts) reused by both runPipeline and backfill. New scripts/backfill.js — pages all VODs, skips already-processed (state/processed-vods.json, gitignored), writes clips/highlights-manifest.json for manual Twitch Highlight creation. Flags: --force, --skip-discord, --dry-run. Dry-run on broadcaster 1025449897 found 3 VODs (198s, 38m, 2h6m). Full backfill deferred to user (~25 min compute).
[2026-05-24 19:30] — Encoding fix: -preset medium -crf 26 -maxrate 2800k -bufsize 5600k (target <22MB for 60s clips, fits Discord 25MB free cap).
[2026-05-24 20:00] — Phase B: Twitch Clip auto-upload via Playwright (user explicitly accepted brittleness). Installed playwright + chromium browser. New scripts/twitch-login.js (interactive, persistent-context profile at state/playwright-profile/). New src/twitch/clipPublisher.js (navigate VOD URL, seek, click clip button, fill title, publish; defensive multi-selector fallback; failure screenshots to state/playwright-screenshots/). New scripts/test-clip-publish.js for single-clip smoke test. reviewBot.sendSummary added for end-of-backfill notification. backfill.js rewritten: dropped per-clip Discord (sendToDiscord: false), now publishes each highlight to Twitch via clipPublisher, writes twitchClipUrl into manifest, posts single Discord summary at end. New flags: --skip-twitch, --headless. IMPORTANT: Twitch's "Create Clip" uses Twitch's own 16:9 VOD as source, not our 9:16 mp4 (no upload-mp4 API exists). Local 9:16 mp4s remain parallel artifacts for manual sharing elsewhere. Selector tuning expected on first live run — user runs twitch-login.js then test-clip-publish.js to validate.
[2026-05-25 01:30] — Live path wired for end-of-stream auto-clip. Rewrote src/pipeline.js runPipeline: now polls getLatestVod every 30s vs processed-vods.json (up to 60min cap for VOD indexing lag), then mirrors backfill pattern (publishClip per highlight, manifest write, mark processed, Discord summary). headless: true. Backfill of 3 unprocessed VODs (2780213254 just-ended 1h48m, 2778522084 38m, 2776267723 2h06m): 45/45 clips published to Twitch, 0 failures, 32m 39s. End-to-end Twitch publish flow validated incl. portrait resize + Save Without Editing dialog. Known bug not yet fixed: backfill/runPipeline mark VOD as processed even on 0-highlight or early error — first ENOENT run locked 3 VODs that had to be manually cleared.
[2026-05-25 02:30] — Phase C: drop local renders, extend slider, TikTok auto-draft. (1) src/pipeline.js: dropped downloadVodSegment + processVideo from processVod; emits stub clip object only (detection + Twitch publish remain). (2) config.js: maxClipLengthSec 60→90, video.maxDurationSec 60→90 (Twitch UI ultimately caps at 60s for Affiliate). (3) src/twitch/clipPublisher.js: new extendClipDuration drags start+end handles outward via mouse (Twitch handles use role=button + draggable-slider, not role=slider; arrow keys do not move them). Iterates up to 6 drags; stops when duration plateaus. Removes ReactModalPortal tooltip subtrees + CLIP_EDITOR_ALERT_BOX after drag. shareToTikTok: clicks share-row TikTok icon, expands collapsed "Export clip to tiktok" section (aria-expanded), then clicks "Send Draft to TikTok" (title left at Twitch default). All post-drag clicks switched to el.evaluate(el=>el.click()) and title fill switched to React-friendly nativeSetter+input event to bypass lingering tooltip portals. Smoke test 2780213254 1000-1090: slider 30s→60s, published=true, tiktokDraftSent=true. Slider hits 60s ceiling (Affiliate cap); 90s would need Partner.
